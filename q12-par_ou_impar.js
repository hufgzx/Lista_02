const prompt = require('prompt-sync')()

function main(){
    const num1 = Number('Número 1: ')
    const num_inteiro = Number('Número inteiro: ')

    const numero_par = (num1 / 2 = num_inteiro)
    const numero_impar = num1 / 2 ≠ num_inteiro)

    if(num1 / 2 = num_inteiro){
        console.log('É par')
    }else (num1 / 2 ≠ num_inteiro){
        console.log('É impar')
    }

}
main()